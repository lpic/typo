<div class=''>
  <h4><%= _("Merge Articles") %></h4>
  <div class='class'>
    <%= _("Article ID: ") %>&nbsp;
    <%= text_field_tag 'merge_with', params[:merge_with], :id => 'merge_with' %><br/>
    <input id='merge_button' type="submit" value="<%= _('Merge') %>" name="merge_button" class='btn info' />
  </div>
</div>
